function OptionIconEl({ children }: { children: React.ReactNode }) {
  return (
    <span className=" w-[50px] flex items-center justify-center ">
      {children}
    </span>
  );
}

export default OptionIconEl;
